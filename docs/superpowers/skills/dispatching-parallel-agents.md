# dispatching-parallel-agents

**触发时机**：面对 2+ 个互相独立的可并行任务。

## 做什么

1. 识别无共享状态的独立任务
2. 为每个任务准备自包含的 prompt
3. 单次消息派发所有 agent
4. 所有 agent 完成后汇总结果

## 关键原则

- 任务必须真正独立（无状态共享）
- 每个 agent 的 prompt 要自包含
- 不要并行派发互斥任务
